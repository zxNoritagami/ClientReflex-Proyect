# Mod Properties
mod_version=1.0.0
maven_group=dev.zxnoragami.clientreflex
archives_base_name=ClientReflex

# Minecraft Properties
minecraft_version=1.21
yarn_mappings=1.21+build.3
loader_version=0.17.2

# Fabric Properties
fabric_version=0.102.0+1.21

# Gradle Performance
org.gradle.jvmargs=-Xmx3G -Xms1G -XX:+UseG1GC -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# Build Environment
# Default: debug (overridable with -Penv=release from command line)
# Example: gradlew build -Penv=release
smartping.env=debug

